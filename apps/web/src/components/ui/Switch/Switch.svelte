<script lang="ts">
	import { Switch } from 'bits-ui';

	interface Props {
		active: boolean;
		action: () => void;
		reverse: () => void;
	}

	let { active, action, reverse }: Props = $props();
</script>

<div class="flex items-center space-x-3">
	<Switch.Root
		checked={active}
		onCheckedChange={(checked) => (checked ? action() : reverse())}
		class="focus-visible:ring-foreground focus-visible:ring-offset-background data-[state=checked]:bg-accent-100 data-[state=unchecked]:bg-main-800 peer border-main-700 inline-flex h-[24px] w-[55px] shrink-0 cursor-pointer items-center border !p-0 px-[3px] transition-colors focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:outline-hidden disabled:cursor-not-allowed disabled:opacity-50"
	>
		<Switch.Thumb
			class="bg-main-50 pointer-events-none block size-[20px] shrink-0 transition-transform data-[state=checked]:translate-x-8 data-[state=unchecked]:translate-x-0"
		/>
	</Switch.Root>
</div>
